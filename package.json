{
  "name": "none",
  "version": "1.0.0",
  "description": "none",
  "main": "index.js",
  "scripts": {
    "test": "mocha './utests/tests.js' && eslint test lib",
    "client": "cd ./front && npm start",
    "server": "nodemon ./server/src/index.js",
    "start": "npm-run-all --parallel server client"
  },
  "devDependencies": {
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "chai": "^4.2.0",
    "mocha": "^7.1.2"
  }
}
